# 论文检索系统使用指南

**生成时间**: 2025-11-14 02:01:27  
**状态**: ✅ 已修复文件名不匹配问题

---

## 📋 快速开始

### 1. 抓取论文数据

```bash
cd /workspace/arxiv-paper-curator

# 方式 1: 抓取昨天的论文（推荐）
python -m src.scripts.fetch_daily_papers_100percent

# 方式 2: 抓取特定日期
python -m src.scripts.fetch_daily_papers_100percent --date 2025-11-13

# 方式 3: 只抓取特定分类（更快）
python -m src.scripts.fetch_daily_papers_100percent --categories cs.AI cs.LG

# 方式 4: 设置自定义输出目录
python -m src.scripts.fetch_daily_papers_100percent --output-dir /workspace/frontend/papers_data
```

**输出文件**:
```
papers_data/papers_2025-11-13_100percent.json
```

### 2. 运行 Streamlit 应用

```bash
cd /workspace/frontend

# 如果论文数据在默认位置 (./papers_data)
streamlit run streamlit_app.py

# 如果论文数据在其他位置
export DATA_DIR=/workspace/arxiv-paper-curator/papers_data
streamlit run streamlit_app.py

# 指定端口
streamlit run streamlit_app.py --server.port 8501
```

### 3. 使用检索功能

访问 `http://localhost:8501`，然后：

1. **选择日期** - 使用日期选择器选择要查看的日期
2. **选择分类** - 在侧边栏勾选感兴趣的分类（如 cs.AI, cs.LG）
3. **输入搜索词** - 在搜索框输入关键词（可选）
4. **查看结果** - 浏览匹配的论文

---

## 🔧 配置说明

### 环境变量

```bash
# 论文数据目录
export DATA_DIR=/path/to/papers_data

# 默认值: ./papers_data
```

### 支持的 ArXiv 分类

- `cs.AI` - Artificial Intelligence
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.LG` - Machine Learning
- `cs.NE` - Neural and Evolutionary Computing
- `cs.CC` - Computational Complexity
- `stat.ML` - Statistics - Machine Learning

---

## 🔍 检索功能说明

### 当前实现的功能

#### 1. 关键词搜索

- **搜索范围**: 论文标题 + 摘要
- **匹配方式**: 子字符串匹配（包含即匹配）
- **大小写**: 不敏感（自动转为小写）

**示例**:
```
搜索 "transformer" 会匹配:
- "Attention Is All You Need: Transformers for NLP"
- "Vision Transformers for Object Detection"
- "Pre-training Transformer Models"
```

#### 2. 分类过滤

- **支持多选**: 可以同时选择多个分类
- **逻辑**: OR 关系（论文属于任一选中分类即显示）

**示例**:
```
选择 cs.AI + cs.LG:
显示所有属于 cs.AI 或 cs.LG 的论文
```

#### 3. 日期选择

- **范围**: 过去一年内的日期
- **格式**: YYYY-MM-DD

#### 4. 组合使用

可以同时使用日期、分类和关键词：

```
日期: 2025-11-13
分类: cs.AI, cs.LG
搜索: "large language model"

结果: 2025-11-13 发布的、属于 cs.AI 或 cs.LG 分类的、
      标题或摘要包含 "large language model" 的论文
```

---

## 📁 文件格式说明

### 输入格式（抓取脚本生成）

**文件名**: `papers_YYYY-MM-DD_100percent.json`

**结构**:
```json
{
  "metadata": {
    "fetch_mode": "100_percent_complete",
    "fetch_date": "2025-11-13T18:30:00",
    "paper_date": "2025-11-12",
    "total_papers": 2385,
    "total_expected": 2385,
    "completeness_status": "100_COMPLETE",
    "all_categories_complete": true,
    "categories": {
      "cs.AI": {
        "category": "cs.AI",
        "papers_fetched": 500,
        "expected_total": 500,
        "completeness": "100%",
        "is_complete": true
      }
    }
  },
  "papers": [
    {
      "arxiv_id": "2411.12345",
      "title": "Paper Title",
      "authors": ["Author 1", "Author 2"],
      "abstract": "Abstract text...",
      "categories": ["cs.AI", "cs.LG"],
      "published_date": "2025-11-12",
      "url": "https://arxiv.org/abs/2411.12345",
      "pdf_url": "https://arxiv.org/pdf/2411.12345.pdf"
    }
  ]
}
```

### 兼容格式（简化版）

**文件名**: `papers_YYYY-MM-DD.json`

**结构**:
```json
[
  {
    "id": "2311.12345",
    "title": "Paper Title",
    "authors": ["Author 1", "Author 2"],
    "abstract": "Abstract text...",
    "categories": ["cs.AI"],
    "published_date": "2025-11-13",
    "url": "https://arxiv.org/abs/2311.12345",
    "pdf_url": "https://arxiv.org/pdf/2311.12345.pdf"
  }
]
```

---

## 🎯 使用场景示例

### 场景 1: 查看昨天的所有 AI 论文

```bash
# 1. 抓取昨天的论文
cd /workspace/arxiv-paper-curator
python -m src.scripts.fetch_daily_papers_100percent --categories cs.AI

# 2. 启动 Streamlit（如果数据在默认位置）
cd /workspace/frontend
streamlit run streamlit_app.py

# 3. 在浏览器中：
#    - 选择昨天的日期
#    - 侧边栏勾选 cs.AI
#    - 浏览所有论文
```

### 场景 2: 搜索特定主题的论文

```bash
# 1. 确保已有论文数据
cd /workspace/frontend
streamlit run streamlit_app.py

# 2. 在浏览器中：
#    - 选择日期: 2025-11-13
#    - 选择分类: cs.AI, cs.LG, cs.CL
#    - 搜索框输入: "large language model"
#    - 点击搜索按钮
```

### 场景 3: 批量抓取多天数据

```bash
cd /workspace/arxiv-paper-curator

# 创建脚本
cat > batch_fetch.sh << 'EOF'
#!/bin/bash
for i in {1..7}; do
  date=$(date -d "$i days ago" +%Y-%m-%d)
  echo "Fetching papers for $date..."
  python -m src.scripts.fetch_daily_papers_100percent \
    --date $date \
    --max-wait-hours 12 \
    --output-dir /workspace/frontend/papers_data
done
EOF

chmod +x batch_fetch.sh
./batch_fetch.sh
```

### 场景 4: 持续运行（自动抓取）

```bash
cd /workspace/arxiv-paper-curator

# 后台持续运行，每天检查一次
nohup python -m src.scripts.fetch_daily_papers_100percent \
  --continuous \
  --check-interval 24 \
  --output-dir /workspace/frontend/papers_data \
  > fetch_100.log 2>&1 &

# 查看日志
tail -f fetch_100.log
```

---

## 🐛 故障排查

### 问题 1: No papers found for date

**原因**:
- 该日期的论文数据文件不存在
- 文件名格式不匹配

**解决**:
```bash
# 检查文件是否存在
ls -la /workspace/frontend/papers_data/

# 运行抓取脚本生成数据
cd /workspace/arxiv-paper-curator
python -m src.scripts.fetch_daily_papers_100percent \
  --date 2025-11-13 \
  --output-dir /workspace/frontend/papers_data
```

### 问题 2: Error loading papers from file

**原因**:
- JSON 格式错误
- 文件权限问题

**解决**:
```bash
# 检查 JSON 格式
cat papers_data/papers_2025-11-13_100percent.json | jq '.' > /dev/null

# 检查权限
chmod 644 papers_data/*.json
```

### 问题 3: Streamlit 无法连接

**原因**:
- 端口被占用
- 防火墙阻止

**解决**:
```bash
# 使用不同端口
streamlit run streamlit_app.py --server.port 8502

# 检查端口
netstat -tuln | grep 8501
```

### 问题 4: 搜索无结果

**原因**:
- 关键词拼写错误
- 搜索词不在标题或摘要中

**建议**:
- 使用更通用的关键词
- 检查拼写
- 尝试搜索同义词

---

## 📊 性能说明

### 抓取性能

| 分类 | 平均论文数 | 预计时间 | 网络要求 |
|------|-----------|---------|---------|
| cs.AI | 500 篇 | 5-15 分钟 | 稳定 |
| cs.LG | 800 篇 | 10-30 分钟 | 稳定 |
| cs.CV | 450 篇 | 5-15 分钟 | 稳定 |
| cs.CL | 300 篇 | 5-10 分钟 | 稳定 |
| 全部 7 个 | ~2500 篇 | 30-120 分钟 | 稳定 |

### 检索性能

| 论文数量 | 搜索时间 | 内存占用 |
|---------|---------|---------|
| 100 篇 | < 0.1s | ~10 MB |
| 1000 篇 | < 0.5s | ~50 MB |
| 10000 篇 | < 2s | ~200 MB |

**注**: 当前使用简单字符串匹配，适合小到中等规模（< 10000 篇）

---

## 🔄 更新日志

### 2025-11-14

✅ **修复**: 文件名不匹配问题
- 支持 `papers_YYYY-MM-DD_100percent.json` 格式
- 支持 `papers_YYYY-MM-DD.json` 格式
- 自动处理包含 metadata 的数据结构

✅ **改进**: 数据加载逻辑
- 更好的错误处理
- 支持多种数据格式
- 更清晰的错误提示

---

## 📚 相关文档

- [100% 数据完整性保证方案](../arxiv-paper-curator/100_PERCENT_GUARANTEE.md)
- [重试机制说明](../arxiv-paper-curator/RETRY_MECHANISM.md)
- [抓取脚本说明](../arxiv-paper-curator/FETCH_DAILY_PAPERS_README.md)

---

## 💡 最佳实践

### 1. 定期抓取

建议每天自动抓取前一天的论文：

```bash
# 添加到 crontab
0 8 * * * cd /workspace/arxiv-paper-curator && python -m src.scripts.fetch_daily_papers_100percent --output-dir /workspace/frontend/papers_data
```

### 2. 数据备份

定期备份论文数据：

```bash
# 创建备份
tar -czf papers_backup_$(date +%Y%m%d).tar.gz papers_data/

# 只保留最近 30 天的备份
find . -name "papers_backup_*.tar.gz" -mtime +30 -delete
```

### 3. 日志管理

保留抓取日志用于调试：

```bash
# 重定向日志
python -m src.scripts.fetch_daily_papers_100percent 2>&1 | tee -a fetch.log

# 只保留最近 7 天的日志
find . -name "*.log" -mtime +7 -delete
```

### 4. 监控

定期检查数据完整性：

```bash
# 检查最近 7 天的数据
for i in {0..6}; do
  date=$(date -d "$i days ago" +%Y-%m-%d)
  file="papers_data/papers_${date}_100percent.json"
  if [ -f "$file" ]; then
    count=$(jq '.papers | length' "$file")
    status=$(jq -r '.metadata.completeness_status' "$file")
    echo "$date: $count papers, $status"
  else
    echo "$date: Missing"
  fi
done
```

---

## 🙋 常见问题

### Q: 为什么默认抓取昨天而不是今天？

A: arXiv 在美国东部时间 20:00 发布当天论文，中国时间约为次日 08:00-09:00。抓取昨天的数据最可靠，不会遗漏。

### Q: 可以同时运行多个抓取实例吗？

A: 
- 不同日期：✅ 可以（checkpoint 不冲突）
- 相同日期：❌ 不建议（会互相覆盖 checkpoint）

### Q: 搜索是否支持正则表达式？

A: 当前版本不支持。如需高级搜索功能，可以扩展 `search_papers` 函数。

### Q: 如何搜索作者？

A: 当前搜索只覆盖标题和摘要。要搜索作者，需要修改代码：

```python
def search_papers(query: str, papers: List[Dict]) -> List[Dict]:
    if not query:
        return papers
    
    query_lower = query.lower()
    results = []
    
    for paper in papers:
        title = paper.get("title", "").lower()
        abstract = paper.get("abstract", "").lower()
        authors = " ".join(paper.get("authors", [])).lower()  # 添加这行
        
        if query_lower in title or query_lower in abstract or query_lower in authors:
            results.append(paper)
    
    return results
```

### Q: 数据会自动更新吗？

A: 不会。需要手动运行抓取脚本，或者使用 `--continuous` 模式自动运行。

---

**文档更新时间**: 2025-11-14 02:01:27  
**版本**: 1.0  
**状态**: ✅ 可用
